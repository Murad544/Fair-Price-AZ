# Local development uses Vite's /api proxy to the existing FastAPI server.
API_PROXY_TARGET=http://127.0.0.1:8000
# For deployment, set the public FastAPI base URL before building.
# VITE_API_BASE_URL=https://your-api.example.com
